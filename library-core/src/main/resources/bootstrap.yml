server:
  port: 9020   # HTTP (Tomcat) port
spring:
  application:
    name: library-core

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka


logging:
  config: classpath:conf/log4j2.xml